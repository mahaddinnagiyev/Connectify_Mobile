import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

export const myFriendsSlice = createSlice({
  name: "myFriends",
  initialState,
  reducers: {},
});

export const {} = myFriendsSlice.actions;
export default myFriendsSlice.reducer;
